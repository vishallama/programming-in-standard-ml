(* Type Bindings *)

type float = real
type count = int and average = real

(* Value bindings *)

val m : int = 3 + 2
val pi : real = 3.1415926 and e : real = 2.178

val x : float = Math.sin pi
val y : float = Math.cos x

